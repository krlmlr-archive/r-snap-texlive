To install:

run
  latex dnaseq.ins

To generate the documentation:

run 
   latex dnaseq.dtx
if your dvi-viewer can not do color specials (e.g. xdvi), 
convert to postscript:
   dvips dnaseq

or run:
   pdflatex dnaseq.dtx














