HA-prosper
----------
HA-prosper is a patch for prosper that adds new
functionality to prosper based presentations.


Installation
------------
This package includes pre-generated run and doc files,
but you can reproduce them from the source if necessary.

The files in `Doc' should go into the `doc' tree of your
LaTeX distribution, the files in `Run' into the `tex' run
tree and the files in `Source' into the source tree.

See for more information about installing packages:
http://www.tex.ac.uk/cgi-bin/texfaq2html?label=instpackages

